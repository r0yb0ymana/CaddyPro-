package caddypro.domain.navcaddy.models

/**
 * Enum representing golf club types.
 *
 * Used for club identification and pattern analysis.
 */
enum class ClubType {
    DRIVER,
    WOOD,
    HYBRID,
    IRON,
    WEDGE,
    PUTTER
}
