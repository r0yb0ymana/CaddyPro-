package com.caddypro.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * CaddyPro Brand Color Palette
 * Based on the brand standards defined in CLAUDE.md
 */
object CaddyProColors {
    // Primary Brand Colors
    val PerformanceLime = Color(0xFF19A849)
    val MatteCharcoal = Color(0xFF1A1A1A)
    val CarbonGrey = Color(0xFF4A4A4A)
    val TitaniumWhite = Color(0xFFF5F5F5)

    // Surface Colors
    val SurfaceElevated = Color(0xFF242424)
    val SurfaceDim = Color(0xFF141414)

    // Border Colors
    val BorderSubtle = Color(0xFF3A3A3A)

    // Semantic Colors
    val DangerZone = Color(0xFFCC3333)
    val Warning = Color(0xFFFFA726)
    val Info = Color(0xFF5C9CE6)

    // Additional shades for variety
    val PerformanceLimeDark = Color(0xFF148036)
    val PerformanceLimeLight = Color(0xFF2DBE5C)
}
